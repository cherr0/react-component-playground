import Button from '../components/Button'
import { Meta, Story, Description, Preview, Props } from '@storybook/addon-docs'

<!-- NOTE
 - title: 그룹명/컴포넌트 이름
 - component: 사용되는 컴포넌트
 - argTypes: 동작 테스트 구성 정보
 -->

<Meta
  title='Components/Button'
  argTypes={{
    color: {
      options: ['primary', 'secondary'],
      control: { type: 'inline-radio' }
    },
    size: {
      options: ['sm', 'md', 'lg', 'xl', 'xxl'],
      control: { type: 'inline-radio' },
      defaultValue: 'sm'
    }
  }}
  component={Button}
/>

export const Template = (args) => {
  return <Button {...args}>Button CTA</Button>
}

# Button

<Description of={Button} />
<Props of={Button} />
